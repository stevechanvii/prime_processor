import React from 'react';

import './Loader.css';

const Timer = () => {
    return (
        <div>
            <div className='loader'>Loading...</div>
        </div>
    );
};

export default Timer;
